/**
 * Installs a new provider.
 *
 * @param {string} name
 */
export function install(name: string) {

}

/**
 * Creates a new log group on the provider.
 *
 * @param {string} name
 */
export function createLogGroup(name: string) {

}
