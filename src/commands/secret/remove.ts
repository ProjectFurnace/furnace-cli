export const command = 'remove <env> <name>';
export const desc = 'Remove secret <name> from environment <env> for currently selected stack';

export function builder(yargs: any) {
  //
}

export function handler(argv: any) {
  //
}
