export const command = 'get <env> <name>';
export const desc = 'Get secret <name> from environment <env> for currently selected stack';

export function builder(yargs: any) {
  //
}

export function handler(argv: any) {
  //
}
