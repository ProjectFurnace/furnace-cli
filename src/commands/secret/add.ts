export const command = 'add <env> <name> <secret>';
export const desc = 'Add secret <name> with value <secret> for environment <env> to currently selected stack';

export function builder(yargs: any) {
  //
}

export function handler(argv: any) {
  //
}
