export const command = 'new <name> <template>';
export const desc = 'Create a module called <name> based on <template>';

export function builder(yargs: any) {
  //
}

export function handler(argv: any) {
  //
}
