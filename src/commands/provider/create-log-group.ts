export const command = 'createLogGroup <name>'
export const desc = 'Creates a new log group with the provider'

export function builder (yargs: any) {
  // 
}

export function handler (argv: any) {
  // 
}
